# Runtime deps (scripts/ pipeline)
PyYAML>=6.0           # formatter writes Clash YAML
httpx>=0.27.0         # crawler / discover_sources unified HTTP client
cachetools>=5.5.0     # verifier result cache
beautifulsoup4>=4.12  # adapters/html page parser
feedparser>=6.0.0     # adapters/rss RSS/Atom feed parser
geoip2>=4.7.0           # verifier 本地地理定位（降级方案仍保留 API 调用）

# Optional: Telegram channel source (scripts/telegram_source.py)
# Not required for other features; only needed when crawling Telegram
telethon>=1.36.0

# Dev / test
ruff>=0.15.20
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
